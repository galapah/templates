[project]
name = "project_name"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = []

[dependency-groups]
dev = [
    "pre-commit",
    "ruff",
    "pytest",
]

[tool.ruff]
line-length = 100
target-version = "py312"
